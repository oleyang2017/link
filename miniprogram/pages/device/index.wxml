<view class="container">
  <view class="category-container">
    <l-tabs equal-width="{{false}}" scrollable animated animatedForLine="{{true}}" active-color='#3863bc'>
      <l-tabpanel wx:for="{{category}}" wx:key="id" tab="{{item.name}}">
        <view class="tab-content"></view>
      </l-tabpanel>
    </l-tabs>
  </view>
</view>

